import { MaintenanceRequest } from './maintenance-request';

describe('MaintenanceRequest', () => {
  it('should create an instance', () => {
    expect(new MaintenanceRequest()).toBeTruthy();
  });
});
