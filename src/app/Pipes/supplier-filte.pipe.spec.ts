import { SupplierFiltePipe } from './supplier-filte.pipe';

describe('SupplierFiltePipe', () => {
  it('create an instance', () => {
    const pipe = new SupplierFiltePipe();
    expect(pipe).toBeTruthy();
  });
});
